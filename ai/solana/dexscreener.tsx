import Image from "next/image"
import { ExternalLink, MoreHorizontal } from "lucide-react"
import { z } from "zod"

import { Badge } from "@/components/ui/badge"
import { Card } from "@/components/ui/card"
import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "@/components/ui/tooltip"
import { capitalize } from "@/lib/utils/format"

// [Include all the interfaces and types from the attachment]

// [Include the OrdersResult, TokenProfile, and TokenProfiles components]

export const dexscreenerTools = {
  getTokenOrders: {
    // [Include the getTokenOrders tool implementation]
  },
  getTokenProfile: {
    // [Include the getTokenProfile tool implementation]
  },
  getLatestTokenProfiles: {
    // [Include the getLatestTokenProfiles tool implementation]
  },
}

